# Spring properties
spring:
  application:
     name: product-service  # Service registers under this name

server.port: 9202   # HTTP (Tomcat) port

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9200/eureka